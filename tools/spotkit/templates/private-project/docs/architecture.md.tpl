# __SPOTKIT_DISPLAY_NAME__ architecture

This profile is a static private HubSpot app. It intentionally excludes the
OAuth runtime and external API generated for marketplace apps. HubSpot issues
the installed private-app token and hosts optional app functions.

Use a separate OAuth marketplace project when an app must be installed in
multiple customer accounts.
