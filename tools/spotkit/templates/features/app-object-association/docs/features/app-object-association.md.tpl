# __SPOTKIT_DISPLAY_NAME__ app-object association

The generated association connects the single app object to contacts. Change
`secondObjectType` before upload if the product instead belongs to companies,
deals, tickets, or another supported CRM object.

The association component depends on the app-object component and the same
HubSpot approval. SpotKit refuses to add it before the app object exists.
