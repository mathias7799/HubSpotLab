# __SPOTKIT_DISPLAY_NAME__ private function

This optional HubSpot-hosted function has no public endpoint and is available
only to compatible private static apps. Invoke it only from supported HubSpot
components.

Declare required secrets in metadata, avoid logging sensitive inputs, and keep
business logic portable when it may later move to another hosting target.
