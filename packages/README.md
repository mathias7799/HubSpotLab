# Packages

Reusable libraries, API clients, schemas, UI components, and shared modules
live here. Packages should expose a deliberate public API and must not depend
on code inside a deployable app.

## Current packages

- `spotkit-runtime`: portable HubSpot OAuth, request-signature validation,
  encrypted installation storage, and runtime routing used by SpotKit-generated
  applications.
