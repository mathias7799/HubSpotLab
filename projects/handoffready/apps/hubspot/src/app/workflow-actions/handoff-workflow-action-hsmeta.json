{
  "uid": "handoffready_prepare_handoff_workflow_action",
  "type": "workflow-action",
  "config": {
    "actionUrl": "https://handoffready.example.com/workflow-actions/prepare-handoff",
    "isPublished": false,
    "supportedClients": [
      {
        "client": "WORKFLOWS"
      }
    ],
    "inputFields": [
      {
        "typeDefinition": {
          "name": "mode",
          "type": "enumeration",
          "fieldType": "select",
          "options": [
            {
              "value": "evaluate",
              "label": "Evaluate only"
            },
            {
              "value": "create_ticket",
              "label": "Create configured handoff when ready"
            }
          ]
        },
        "supportedValueTypes": ["STATIC_VALUE"],
        "isRequired": true
      },
      {
        "typeDefinition": {
          "name": "route_id",
          "type": "string",
          "fieldType": "text"
        },
        "supportedValueTypes": ["STATIC_VALUE"],
        "isRequired": false
      }
    ],
    "outputFields": [
      {
        "typeDefinition": {
          "name": "status",
          "type": "string",
          "fieldType": "text"
        }
      },
      {
        "typeDefinition": {
          "name": "missing_count",
          "type": "number",
          "fieldType": "number"
        }
      },
      {
        "typeDefinition": {
          "name": "ticket_id",
          "type": "string",
          "fieldType": "text"
        }
      },
      {
        "typeDefinition": {
          "name": "output_ids",
          "type": "string",
          "fieldType": "text"
        }
      }
    ],
    "labels": {
      "en": {
        "actionName": "HandoffReady: prepare customer handoff",
        "actionDescription": "Evaluate a closed-won deal and optionally create a configured department handoff.",
        "actionCardContent": "Prepare the customer handoff",
        "inputFieldLabels": {
          "mode": "Handoff action",
          "route_id": "Handoff route ID"
        },
        "inputFieldDescriptions": {
          "mode": "Choose whether to evaluate readiness or create the configured output when all prerequisites pass.",
          "route_id": "Optional stable route ID from HandoffReady settings. Leave blank to use the primary route."
        },
        "outputFieldLabels": {
          "status": "Handoff status",
          "missing_count": "Missing requirement count",
          "ticket_id": "Created or existing ticket ID",
          "output_ids": "Created or existing output IDs"
        }
      }
    },
    "objectTypes": ["DEAL"]
  }
}
