# Examples

Minimal, runnable HubSpot API and extension examples live here. Examples should
favor clarity over abstraction, use synthetic data, and provide an
`.env.example` when configuration is needed.
